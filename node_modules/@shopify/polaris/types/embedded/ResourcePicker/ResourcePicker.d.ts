import * as React from 'react';
import { WithAppProviderProps } from '../../components/AppProvider';
import { OpenOptions } from '../easdk/components/ResourcePicker';
export interface Props extends OpenOptions {
    /** Whether the picker is open or not */
    open: boolean;
}
export declare class ResourcePicker extends React.PureComponent<Props & WithAppProviderProps, never> {
    private focusReturnPoint;
    componentDidMount(): void;
    componentDidUpdate({ open: wasOpen }: Props): void;
    render(): null;
    private handleEASDKMessaging;
}
declare const _default: React.ComponentClass<Props> & typeof ResourcePicker;
export default _default;
