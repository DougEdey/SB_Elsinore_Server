import * as React from 'react';
import { WithAppProviderProps } from '../../../../components/AppProvider';
export interface Props {
    accessibilityLabel?: string;
    label?: string;
    selected?: boolean | 'indeterminate';
    selectMode?: boolean;
    plain?: boolean;
    measuring?: boolean;
    onToggleAll?(): void;
}
export declare type CombinedProps = Props & WithAppProviderProps;
declare function CheckableButton({ accessibilityLabel, label, onToggleAll, selected, selectMode, plain, measuring, }: CombinedProps): JSX.Element;
declare const _default: React.ComponentClass<Props> & typeof CheckableButton;
export default _default;
