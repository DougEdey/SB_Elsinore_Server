import * as React from 'react';
export interface Props {
    fixed?: boolean;
    sectioned?: boolean;
    children?: React.ReactNode;
}
export default function Pane({ fixed, sectioned, children }: Props): JSX.Element;
