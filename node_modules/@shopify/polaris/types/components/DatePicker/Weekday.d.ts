/// <reference types="react" />
import { Weekdays } from '@shopify/javascript-utilities/dates';
export interface Props {
    label: Weekdays;
    title: string;
    current: boolean;
}
export default function Weekday({ label, title, current }: Props): JSX.Element;
