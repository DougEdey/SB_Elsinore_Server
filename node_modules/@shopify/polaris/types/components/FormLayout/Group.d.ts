import * as React from 'react';
export interface Props {
    children?: React.ReactNode;
    condensed?: boolean;
    title?: string;
    helpText?: React.ReactNode;
}
export default function Group({ children, condensed, title, helpText }: Props): JSX.Element;
