/// <reference types="react" />
import { ItemDescriptor } from './types';
export declare type Props = ItemDescriptor;
export default function Item({ badge, content, url, onAction, icon, image, disabled, external, ellipsis, }: Props): JSX.Element;
