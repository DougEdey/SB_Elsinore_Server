import * as React from 'react';
import { DisableableAction } from '../../types';
export interface Props {
    children?: React.ReactNode;
    actions?: DisableableAction[];
}
export default function Header({ children, actions }: Props): JSX.Element;
