import * as React from 'react';
export interface Props {
    trapping?: boolean;
    children?: React.ReactNode;
}
export default class TrapFocus extends React.PureComponent<Props, never> {
    private focusTrapWrapper;
    render(): JSX.Element;
    private setFocusTrapWrapper;
    private handleBlur;
}
