import * as React from 'react';
import { AnimationProps } from '../../../../types';
export interface DialogProps {
    labelledBy: string;
    instant?: boolean;
    children?: React.ReactNode;
    limitHeight?: boolean;
    large?: boolean;
    onClose(): void;
    onEntered?(): void;
    onExited?(): void;
}
export declare type Props = DialogProps & AnimationProps;
export default function Dialog({ instant, labelledBy, children, onClose, onExited, onEntered, large, limitHeight, ...props }: Props): JSX.Element;
