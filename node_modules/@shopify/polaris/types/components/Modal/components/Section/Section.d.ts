import * as React from 'react';
export interface Props {
    children?: React.ReactNode;
    flush?: boolean;
    subdued?: boolean;
}
export default function Section({ children, flush, subdued, }: Props): JSX.Element;
