import * as React from 'react';
export interface Props {
    id: string;
    tabID: string;
    children?: React.ReactNode;
}
export default function Panel({ id, tabID, children }: Props): JSX.Element;
