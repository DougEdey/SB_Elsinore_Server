import * as React from 'react';
export interface Props {
    action?: React.ReactNode;
    children?: React.ReactNode;
}
export default function SettingAction({ action, children }: Props): JSX.Element;
