/// <reference types="react" />
export interface Props {
    checked?: boolean;
    disabled?: boolean;
    id?: string;
    name?: string;
    value?: string;
    onChange(): void;
}
export default function Checkbox({ id, checked, disabled, onChange, name, value, }: Props): JSX.Element;
