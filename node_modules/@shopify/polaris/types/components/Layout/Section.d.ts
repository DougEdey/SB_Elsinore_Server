import * as React from 'react';
export interface Props {
    children?: React.ReactNode;
    secondary?: boolean;
    fullWidth?: boolean;
}
export default function Section({ children, secondary, fullWidth }: Props): JSX.Element;
