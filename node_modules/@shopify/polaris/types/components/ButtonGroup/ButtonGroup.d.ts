import * as React from 'react';
export interface Props {
    /** Join buttons as segmented group */
    segmented?: boolean;
    /** Button components */
    children?: React.ReactNode;
}
export default function ButtonGroup({ children, segmented }: Props): JSX.Element;
